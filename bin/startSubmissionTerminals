#!/bin/bash

hosts="klin klin klin klin"

hosts=($hosts)
lastHost=${hosts[@]:(-1)}
unset hosts[${#hosts[@]}-1]

for host in ${hosts[@]}
do
  gnome-terminal -e "ssh $host"
  sleep 1
done
ssh $lastHost
